---
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>Tokage Downloads</title>
    <meta
      name="description"
      content="Download the latest Tokage macOS build and see version details."
    />
    <style>
      @import url("https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,500;9..144,700&family=Space+Grotesk:wght@400;500;600&display=swap");

      :root {
        color-scheme: light;
        --paper: #f5f0e6;
        --paper-strong: #fbf8f1;
        --ink: #0d141b;
        --ink-subtle: #3b4551;
        --ink-muted: #6a747f;
        --accent: #0c8e6f;
        --accent-strong: #075d4a;
        --accent-glow: rgba(17, 141, 111, 0.18);
        --shadow: 0 22px 60px rgba(13, 20, 27, 0.18);
        --radius: 22px;
      }

      * {
        box-sizing: border-box;
      }

      body {
        margin: 0;
        min-height: 100vh;
        font-family: "Space Grotesk", sans-serif;
        color: var(--ink);
        background: radial-gradient(circle at 12% 18%, rgba(9, 125, 97, 0.2), transparent 52%),
          radial-gradient(circle at 86% 22%, rgba(224, 142, 46, 0.18), transparent 48%),
          linear-gradient(120deg, #f4ede1 0%, #f0f7f5 52%, #fbf8f1 100%);
        background-attachment: fixed;
      }

      header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 24px 6vw 0;
        max-width: 1180px;
        margin: 0 auto;
      }

      .logo {
        font-weight: 600;
        letter-spacing: 0.04em;
        text-transform: uppercase;
        font-size: 0.9rem;
      }

      nav {
        display: flex;
        gap: 18px;
        font-size: 0.9rem;
      }

      nav a {
        color: var(--ink-muted);
        text-decoration: none;
      }

      nav a:hover {
        color: var(--ink);
      }

      main {
        display: grid;
        gap: 48px;
        padding: 48px 6vw 96px;
        max-width: 980px;
        margin: 0 auto;
      }

      .hero {
        display: grid;
        gap: 18px;
      }

      .eyebrow {
        text-transform: uppercase;
        letter-spacing: 0.16em;
        font-size: 0.75rem;
        color: var(--ink-muted);
      }

      h1 {
        font-family: "Fraunces", serif;
        font-size: clamp(2.1rem, 4vw, 3.4rem);
        line-height: 1.05;
        margin: 0;
      }

      p {
        margin: 0;
        color: var(--ink-subtle);
        line-height: 1.7;
      }

      .card {
        background: var(--paper-strong);
        border-radius: var(--radius);
        padding: 28px;
        box-shadow: var(--shadow);
        border: 1px solid rgba(13, 20, 27, 0.08);
        position: relative;
        overflow: hidden;
      }

      .card::before {
        content: "";
        position: absolute;
        inset: 16px;
        border-radius: 18px;
        background: radial-gradient(circle at top left, var(--accent-glow), transparent 65%);
        opacity: 0.8;
      }

      .card-content {
        position: relative;
        display: grid;
        gap: 18px;
      }

      .download-meta {
        display: grid;
        gap: 10px;
        font-size: 0.95rem;
        color: var(--ink-subtle);
      }

      .download-meta strong {
        color: var(--accent-strong);
        font-weight: 600;
      }

      .actions {
        display: flex;
        flex-wrap: wrap;
        gap: 14px;
        align-items: center;
      }

      .button {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        padding: 12px 20px;
        border-radius: 999px;
        background: var(--accent);
        color: white;
        text-decoration: none;
        font-weight: 600;
        box-shadow: 0 16px 40px rgba(12, 142, 111, 0.3);
      }

      .button:hover {
        background: var(--accent-strong);
      }

      .secondary-link {
        color: var(--ink-muted);
        text-decoration: none;
        font-size: 0.95rem;
      }

      .secondary-link:hover {
        color: var(--ink);
      }

      .note {
        font-size: 0.9rem;
        color: var(--ink-muted);
      }

      @media (max-width: 720px) {
        header {
          flex-direction: column;
          gap: 12px;
          align-items: flex-start;
        }
      }
    </style>
  </head>
  <body>
    <header>
      <div class="logo">Tokage</div>
      <nav>
        <a href="/">Overview</a>
        <a href="/release-notes/1.0">Release notes</a>
        <a href="/appcast.xml">Appcast</a>
      </nav>
    </header>

    <main>
      <section class="hero">
        <div class="eyebrow">macOS download</div>
        <h1>Download Tokage for your menu bar.</h1>
        <p>
          The latest public build includes real-time token usage summaries, daily breakdowns,
          and local-only log parsing for Codex usage tracking.
        </p>
      </section>

      <section class="card">
        <div class="card-content">
          <h2>Tokage 1.0</h2>
          <div class="download-meta">
            <div>Version: <strong>1.0</strong></div>
            <div>Format: <strong>macOS DMG</strong></div>
            <div>Size: <strong>3.1 MB</strong></div>
          </div>
          <div class="actions">
            <a class="button" href="/downloads/Tokage.dmg">Download DMG</a>
            <a class="secondary-link" href="/release-notes/1.0">Read release notes</a>
          </div>
          <div class="note">You can replace the DMG at `/downloads/Tokage.dmg` when a new build is ready.</div>
        </div>
      </section>
    </main>
  </body>
</html>
